import InputBase from '@material-ui/core/InputBase';
import React from 'react';

const HeroToolbarInput = props => <InputBase placeholder="Select a hero" {...props} />;

export default HeroToolbarInput;
